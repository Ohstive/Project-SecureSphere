<Page x:Class="SecureSphereV2.D.DistanceAcess"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SecureSphereV2"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DistanceAcess"
      Background="White">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="204" Height="146">
            <CheckBox x:Name="showPasswordCheckBox" Content="Afficher Mot de passe" Checked="ShowPasswordCheckBox_Checked" Unchecked="ShowPasswordCheckBox_Unchecked"/>
            <TextBox x:Name="passwordTextBox" Width="200" Margin="0,0,0,10" Text="{Binding SavedPassword, Mode=TwoWay}" />
            <PasswordBox x:Name="passwordBox" Width="200" Margin="0,0,0,10" Visibility="Collapsed"/>

            <!-- Ajoutez le bouton SavePassword -->
            <Button Content="Enregistrer le Mot de passe" Click="SavePassword_Click" Margin="0,0,0,10"/>
        </StackPanel>
    </Grid>
</Page>
